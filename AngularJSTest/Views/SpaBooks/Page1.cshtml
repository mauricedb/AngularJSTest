<div class="container">
    <h4>This page displays each book from a client side array</h4>
    <div class="span3 well">
        <ul class="nav">
            <li ng-repeat="book in books" ng-click="select(book)" class="btn-link">{{book.title}}</li>
        </ul>

    </div>
    <div class="span8">
        Id: {{selected.id}}
            <br />
        Title: {{selected.title}}
            <br />
        Author: {{selected.author}}

    </div>
</div>
